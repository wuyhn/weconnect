<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Weconnect"
        android:usesCleartextTraffic="true">

        <activity
            android:name="com.example.weconnect.presentation.auth.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.weconnect.presentation.auth.register.RegisterActivity"
            android:exported="false" />

        <activity
            android:name="com.example.weconnect.presentation.main.MainActivity"
            android:exported="false" />
        <activity
            android:name="com.example.weconnect.presentation.post.create.CreatePostActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".presentation.search.SearchActivity" />
        <activity android:name=".presentation.post.archive.ArchivePostsActivity" />
        <activity android:name=".presentation.chat.list.ChatListActivity" />
        <activity android:name=".presentation.chat.conversation.ConversationActivity" />
        <activity android:name=".presentation.profile.view.UserProfileActivity" />
        <activity android:name=".presentation.post.detail.PostDetailActivity" />
        <activity android:name=".presentation.profile.edit.EditProfileActivity" />
        <activity android:name=".presentation.auth.changepassword.ChangePasswordActivity" />
        <activity android:name=".presentation.social.participants.ParticipantsActivity" />
        <activity android:name=".presentation.notification.NotificationsActivity" />
        <activity android:name=".presentation.onboarding.OnboardingActivity" />
        <activity android:name=".presentation.auth.forgotpassword.ForgotPasswordActivity" />
        <activity android:name=".presentation.social.blocked.BlockedUsersActivity" />
        <activity android:name=".presentation.legal.LegalActivity" />
        <activity android:name=".presentation.social.pending.PendingListActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
